<!--
  ~ Copyright (C) 2016-2017 Lightbend Inc. <https://www.lightbend.com>
  -->
<!-- Based on the default logback configuration that Lagom uses in dev mode -->
<configuration>

    <conversionRule conversionWord="coloredLevel" converterClass="com.lightbend.lagom.internal.logback.ColoredLevel" />

    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>${application.home:-.}/logs/application.log</file>
        <encoder>
            <pattern>%date [%level] from %logger in %thread - %message%n%xException</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%coloredLevel %logger{15} - %message%n%xException{10}</pattern>
        </encoder>
    </appender>

    <!-- Set logging for all Play library classes to WARN -->
    <logger name="play" level="WARN" />
    <!-- Set logging for all Akka library classes to WARN -->
    <logger name="akka" level="WARN" />
    <!-- Set logging for all Lagom library classes to WARN -->
    <logger name="com.lightbend.lagom" level="WARN" />

    <!-- Override that for classes in this project -->
    <logger name="com.lightbend.lagom.messagehub" level="INFO" />

    <!-- Increase Kafka log level to INFO since this project is demonstrating Kafka connectivity -->
    <logger name="org.apache.kafka" level="INFO" />

    <root level="INFO">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="FILE" />
    </root>

</configuration>
